$(document).ready(function () {    sessao();    notify();    $("#btnPrint").live("click", function () {        var divContents = $("#conteudo-pedido").html();        var printWindow = window.open('', '', 'height=600,width=800');        printWindow.document.write('<link rel="stylesheet" href="/adm/default/css/bootstrap.css" type="text/css" />');        printWindow.document.write('<link rel="stylesheet" href="/adm/default/css/custom.css" type="text/css" />');        printWindow.document.write('<html><head><title>PEDIDO Nº ' + $(this).data('pedido') + '</title>');        printWindow.document.write('</head><body >');        printWindow.document.write(divContents);        printWindow.document.write('</body></html>');        printWindow.document.close();        printWindow.print();    });    $('#rendimento, #mesRendimento, #anoRendimento').change(function () {        var categoria = $('#rendimento').val();        $('.rendimento').html('<div class="progress"><div class="progress-bar progress-bar-info progress-bar-striped" role="progressbar" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100" style="width: 20%"><span class="sr-only">20% Complete</span></div></div>');        $.ajax({            url: '/admin/relatorios/ajax-rendimentos/',            type: 'POST',            dataType: 'html',            data: {                categoria: categoria,                mes: $('#mesRendimento').val(),                ano: $('#anoRendimento').val()            }        }).done(function (data) {            $('.rendimento').html(data);        });    });    $('#despesa, #mesDespesa, #anoDespesa').change(function () {        var categoria = $('#despesa').val();        $('.despesa').html('<div class="progress"><div class="progress-bar progress-bar-info progress-bar-striped" role="progressbar" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100" style="width: 20%"><span class="sr-only">20% Complete</span></div></div>');        $.ajax({            url: '/admin/relatorios/ajax-despesas/',            type: 'POST',            dataType: 'html',            data: {                categoria: categoria,                mes: $('#mesDespesa').val(),                ano: $('#anoDespesa').val()            }        }).done(function (data) {            $('.despesa').html(data);        });    });    $('#mesLiquido, #anoLiquido').change(function () {        $('.liquido').html('<div class="progress"><div class="progress-bar progress-bar-info progress-bar-striped" role="progressbar" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100" style="width: 20%"><span class="sr-only">20% Complete</span></div></div>');        $.ajax({            url: '/admin/relatorios/ajax-liquido/',            type: 'POST',            dataType: 'html',            data: {                mes: $('#mesLiquido').val(),                ano: $('#anoLiquido').val()            }        }).done(function (data) {            $('.liquido').html(data);        });    });});function sessao() {    $.ajax({        url: '/admin/index/ajax-sesao/',        type: 'POST',        dataType: 'json'    }).done(function (data) {    });}function notify() {    if ($('#NotificaDados').length) {        Notification.requestPermission(function () {            var notification = new Notification("Olá " + $('#NotificaDados').data('nome'), {                icon: $('#NotificaDados').data('img'),                body: "Seu último acesso foi: " + $('#NotificaDados').data('acesso') + " pelo endereço de IP: " + $('#NotificaDados').data('ip')            });            /* notification.onclick = function() {             window.open("http://stackoverflow.com");             */        });    }}function number_format(number, decimals, dec_point, thousands_sep) {    var n = number, prec = decimals;    n = !isFinite(+n) ? 0 : +n;    prec = !isFinite(+prec) ? 0 : Math.abs(prec);    var sep = (typeof thousands_sep == "undefined") ? ',' : thousands_sep;    var dec = (typeof dec_point == "undefined") ? '.' : dec_point;    var s = (prec > 0) ? n.toFixed(prec) : Math.round(n).toFixed(prec); //fix for IE parseFloat(0.55).toFixed(0) = 0;    var abs = Math.abs(n).toFixed(prec);    var _, i;    if (abs >= 1000) {        _ = abs.split(/\D/);        i = _[0].length % 3 || 3;        _[0] = s.slice(0, i + (n < 0)) +                _[0].slice(i).replace(/(\d{3})/g, sep + '$1');        s = _.join(dec);    } else {        s = s.replace('.', dec);    }    return s;}